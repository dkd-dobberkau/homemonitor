services:
  collector:
    build: .
    image: homemonitor-collector
    container_name: homemonitor-collector
    volumes:
      - ./data:/app/data
      - ./config.ini:/app/config.ini:ro
    restart: unless-stopped

  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    image: homemonitor-dashboard
    container_name: homemonitor-dashboard
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
    depends_on:
      - collector
